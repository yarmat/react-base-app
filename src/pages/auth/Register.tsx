import React, {FC} from 'react';
import {Card} from "antd";

const Register: FC = () => {
    return (
        <Card title="Register">
            form
        </Card>
    );
};

export default Register;
